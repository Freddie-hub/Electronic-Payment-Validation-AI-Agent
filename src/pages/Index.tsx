import { ChatPage } from './ChatPage';

/**
 * Main index page - EPS Agent Chat Interface
 * Entry point for the local AI task processing system
 */
const Index = () => {
  return <ChatPage />;
};

export default Index;
